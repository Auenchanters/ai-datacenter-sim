# Action parser.
# Accepts AI output JSON, validates commands against hardware catalog,
# verifies budget constraints, and executes placements on the grid.
# Valid commands: BUY_EQUIPMENT, ADJUST_COOLING, ACCEPT_CONTRACT, ROUTE_WORKLOAD.
