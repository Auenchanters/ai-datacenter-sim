# LLMAgent: integrates with any LLM via LiteLLM.
# Sends system prompt + state JSON to the target model.
# Parses the model response and returns a validated action payload.
# Supports: openai/gpt-4o, anthropic/claude-3-5-sonnet, meta/llama-3, and local models.
